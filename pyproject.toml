[project]
name = "qiskit-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.10.6",
    "qiskit>=2.1.1",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]
